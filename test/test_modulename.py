import unittest

from big_gene_table.modulename import functionname

class TestModulename(unittest.TestCase):

    def test_functionname(self):
        self.assertEqual(
            functionname(
                1, 1
                ),
                2
            )
